"""
Day 11 — Pandas DataFrame পরিচিতি (Pandas DataFrame Introduction)
===================================================================
লেখক: Jahirul Islam
বিষয়: DataFrame — ডিকশনারি/CSV থেকে তৈরি, head(), info(), কলাম/রো সিলেক্ট
ফাইন্যান্স উদাহরণ: পোর্টফোলিও DataFrame (Portfolio DataFrame)

কি শিখবো:
  1. DataFrame তৈরি (dict, list, CSV থেকে)
  2. head(), tail(), info(), describe()
  3. কলাম সিলেক্ট ও অ্যাড করা
  4. রো সিলেক্ট (loc, iloc)
  5. ফাইন্যান্সিয়াল ডেটা নিয়ে কাজ
"""

import pandas as pd
import numpy as np

# ============================================================
# ১. DataFrame তৈরি (Creating a DataFrame)
# ============================================================

print("=" * 60)
print("১. DataFrame তৈরি")
print("=" * 60)

# ডিকশনারি থেকে — সবচেয়ে সাধারণ উপায়
portfolio_data = {
    'কোম্পানি':   ['Apple', 'Google', 'Microsoft', 'Amazon', 'Tesla'],
    'টিকার':      ['AAPL', 'GOOGL', 'MSFT', 'AMZN', 'TSLA'],
    'শেয়ার সংখ্যা': [100, 50, 75, 30, 45],
    'প্রতি শেয়ার মূল্য': [175.50, 140.20, 380.15, 178.25, 245.60],
    'সেক্টর':     ['টেক', 'টেক', 'টেক', 'ই-কমার্স', 'অটো']
}

df = pd.DataFrame(portfolio_data)
print("পোর্টফোলিও DataFrame:")
print(df)
print()

print("DataFrame-এর টাইপ:", type(df))
print("মাত্রা (shape):", df.shape)
print()

# ============================================================
# ২. DataFrame এক্সপ্লোর (Exploring DataFrame)
# ============================================================

print("=" * 60)
print("২. DataFrame এক্সপ্লোর করা")
print("=" * 60)

# head() — প্রথম কয়েক রো
print("head() — প্রথম ৩ রো:")
print(df.head(3))
print()

# tail() — শেষ কয়েক রো
print("tail() — শেষ ২ রো:")
print(df.tail(2))
print()

# info() — ডেটা টাইপ, নাল ভ্যালু, মেমরি ইউসেজ
print("info() — ডেটার ইনফরমেশন:")
print(df.info())
print()

# describe() — সংখ্যাসূচক কলামের পরিসংখ্যান
print("describe() — পরিসংখ্যান:")
print(df.describe())
print()

# dtypes — প্রতি কলামের ডেটা টাইপ
print("dtypes — কলামের ডেটা টাইপ:")
print(df.dtypes)
print()

# columns — কলামের নাম
print("columns — কলামের তালিকা:")
print(df.columns.tolist())
print()

# index — রো ইনডেক্স
print("index — রো ইনডেক্স:")
print(df.index)
print()

# ============================================================
# ৩. কলাম সিলেক্ট করা (Selecting Columns)
# ============================================================

print("=" * 60)
print("৩. কলাম সিলেক্ট করা")
print("=" * 60)

# একক কলাম — Series রিটার্ন করে
prices = df['প্রতি শেয়ার মূল্য']
print("প্রতি শেয়ার মূল্য কলাম (Series):")
print(prices)
print("টাইপ:", type(prices))
print()

# একাধিক কলাম — DataFrame রিটার্ন করে
selected = df[['কোম্পানি', 'টিকার', 'প্রতি শেয়ার মূল্য']]
print("একাধিক কলাম (DataFrame):")
print(selected)
print()

# ডট নোটেশন (শুধু স্পেস/বাংলা ছাড়া নাম থাকলে)
# যেমন: df.টিকার — বাংলায় কাজ নাও করতে পারে, তাই [] ব্যবহার করাই নিরাপদ
print()

# ============================================================
# ৪. নতুন কলাম যোগ করা (Adding New Columns)
# ============================================================

print("=" * 60)
print("৪. নতুন কলাম যোগ করা")
print("=" * 60)

# মোট বিনিয়োগ = শেয়ার সংখ্যা × প্রতি শেয়ার মূল্য
df['মোট বিনিয়োগ'] = df['শেয়ার সংখ্যা'] * df['প্রতি শেয়ার মূল্য']
print("মোট বিনিয়োগ কলাম যোগ করার পর:")
print(df)
print()

# পোর্টফোলিওতে শতাংশ ওয়েটেজ
total_investment = df['মোট বিনিয়োগ'].sum()
df['ওয়েটেজ (%)'] = (df['মোট বিনিয়োগ'] / total_investment) * 100
print("ওয়েটেজ কলাম যোগ করার পর:")
print(df)
print()

# ডেইলি রিটার্ন (ধরি, আজকের পরে ২% বেড়েছে)
df['নতুন মূল্য'] = df['প্রতি শেয়ার মূল্য'] * 1.02
df['দিন পরিবর্তন ($)'] = df['নতুন মূল্য'] - df['প্রতি শেয়ার মূল্য']
print("প্রাইস পরিবর্তন কলাম যোগ করার পর:")
print(df)
print()

# ============================================================
# ৫. রো সিলেক্ট করা (Selecting Rows)
# ============================================================

print("=" * 60)
print("৫. রো সিলেক্ট করা")
print("=" * 60)

# iloc — পজিশনাল ইনডেক্স (০ থেকে শুরু)
print("প্রথম রো (iloc[0]):")
print(df.iloc[0])
print()

print("প্রথম ৩ রো (iloc[:3]):")
print(df.iloc[:3])
print()

print("শেষ ২ রো (iloc[-2:]):")
print(df.iloc[-2:])
print()

print("২য় থেকে ৪র্থ রো, নির্দিষ্ট কলাম:")
print(df.iloc[1:4, [0, 1, 3]])      # সারি 1-3, কলাম 0,1,3
print()

# loc — লেবেল-ভিত্তিক ইনডেক্স
print("ইনডেক্স ২-এর রো (loc[2]):")
print(df.loc[2])
print()

print("ইনডেক্স ০ থেকে ২ পর্যন্ত:")
print(df.loc[0:2, ['কোম্পানি', 'টিকার', 'মোট বিনিয়োগ']])
print()

# ============================================================
# ৬. ফিল্টারিং (Filtering Rows by Condition)
# ============================================================

print("=" * 60)
print("৬. ফিল্টারিং — শর্ত দিয়ে রো বাছাই")
print("=" * 60)

# শুধু টেক সেক্টরের কোম্পানি
tech_stocks = df[df['সেক্টর'] == 'টেক']
print("টেক সেক্টরের কোম্পানি:")
print(tech_stocks)
print()

# মোট বিনিয়োগ ১০০০০-এর বেশি
big_investments = df[df['মোট বিনিয়োগ'] > 10000]
print("১০০০০-এর বেশি বিনিয়োগ:")
print(big_investments)
print()

# একাধিক শর্ত (& = এবং, | = বা)
filtered = df[(df['সেক্টর'] == 'টেক') & (df['শেয়ার সংখ্যা'] >= 50)]
print("টেক সেক্টর ও ৫০+ শেয়ার:")
print(filtered)
print()

# query() মেথড — সহজ উপায়
print("query() ব্যবহার:")
print(df.query('`মোট বিনিয়োগ` > 15000'))
print()

# ============================================================
# ৭. ফাইন্যান্সিয়াল উদাহরণ: পোর্টফোলিও বিশ্লেষণ
# ============================================================

print("=" * 60)
print("৭. পোর্টফোলিও বিশ্লেষণ")
print("=" * 60)

print("সম্পূর্ণ পোর্টফোলিও:")
print(df)
print()

# পোর্টফোলিও সামারি
print(f"মোট বিনিয়োগ: ${total_investment:,.2f}")
print(f"গড় বিনিয়োগ: ${df['মোট বিনিয়োগ'].mean():,.2f}")
print(f"সবচেয়ে বড় বিনিয়োগ: ${df['মোট বিনিয়োগ'].max():,.2f}")
print(f"সবচেয়ে ছোট বিনিয়োগ: ${df['মোট বিনিয়োগ'].min():,.2f}")
print()

# সেক্টর অনুযায়ী গ্রুপ সারসংক্ষেপ
sector_group = df.groupby('সেক্টর')['মোট বিনিয়োগ'].sum()
print("সেক্টর অনুযায়ী মোট বিনিয়োগ:")
print(sector_group)
print()

# সর্ট করা
print("বিনিয়োগ অনুযায়ী সর্টেড (descending):")
sorted_df = df.sort_values('মোট বিনিয়োগ', ascending=False)
print(sorted_df[['কোম্পানি', 'টিকার', 'মোট বিনিয়োগ', 'ওয়েটেজ (%)']])
print()

# ============================================================
# ৮. CSV ফাইল থেকে DataFrame তৈরি (সিমুলেটেড)
# ============================================================

print("=" * 60)
print("৮. CSV ফাইল থেকে DataFrame তৈরি")
print("=" * 60)

# একটি সিমুলেটেড CSV তৈরি করছি (রিয়েল প্রজেক্টে pd.read_csv() ব্যবহার করবেন)
csv_data = """তারিখ,স্টক,ওপেন,হাই,লো,ক্লোজ,ভলিউম
2024-01-01,AAPL,150.00,152.50,148.75,151.25,100000
2024-01-01,GOOGL,135.00,137.20,133.80,136.50,85000
2024-01-01,MSFT,370.00,375.00,368.50,373.20,72000
2024-01-02,AAPL,151.25,153.00,149.50,152.10,110000
2024-01-02,GOOGL,136.50,138.00,135.20,137.10,78000
2024-01-02,MSFT,373.20,378.00,372.00,376.80,69000
"""

# String থেকে DataFrame (pd.read_csv(ফাইল_পাথ) দিয়ে ফাইল থেকে পড়বেন)
from io import StringIO
market_data = pd.read_csv(StringIO(csv_data))
print("মার্কেট ডেটা DataFrame:")
print(market_data)
print()

print("head():")
print(market_data.head())
print()

print("info():")
print(market_data.info())
print()

# নির্দিষ্ট স্টকের ডেটা ফিল্টার
aapl_data = market_data[market_data['স্টক'] == 'AAPL']
print("শুধু AAPL-এর ডেটা:")
print(aapl_data)
print()

# গড় ক্লোজিং প্রাইস (স্টক অনুযায়ী)
avg_close = market_data.groupby('স্টক')['ক্লোজ'].mean()
print("স্টক অনুযায়ী গড় ক্লোজিং প্রাইস:")
print(avg_close)
print()

# ============================================================
# ৯. DataFrame-এ দরকারি অপারেশন
# ============================================================

print("=" * 60)
print("৯. দরকারি DataFrame অপারেশন")
print("=" * 60)

# rename() — কলামের নাম পরিবর্তন
df_renamed = df.rename(columns={
    'প্রতি শেয়ার মূল্য': 'বর্তমান মূল্য',
    'মোট বিনিয়োগ': 'বিনিয়োগের পরিমাণ'
})
print("কলামের নাম পরিবর্তনের পর:")
print(df_renamed.columns.tolist())
print()

# drop() — কলাম/রো ডিলিট
df_dropped = df.drop(columns=['নতুন মূল্য', 'দিন পরিবর্তন ($)'])
print("কলাম ড্রপ করার পর:")
print(df_dropped.columns.tolist())
print()

# copy() — DataFrame-এর কপি তৈরি (অরিজিনাল সুরক্ষিত)
df_copy = df.copy()
print("df_copy তৈরি করা হয়েছে — অরিজিনাল অপরিবর্তিত থাকবে")
print()

# ============================================================
# প্র্যাকটিস টাস্ক (Practice Tasks)
# ============================================================
print("=" * 60)
print("🎯 প্র্যাকটিস টাস্ক")
print("=" * 60)
print("""
টাস্ক ১: ৮টি কোম্পানির একটি পোর্টফোলিও DataFrame তৈরি করুন।
         কলাম: কোম্পানি, টিকার, সেক্টর, শেয়ার সংখ্যা, প্রাইস।
         মোট বিনিয়োগ এবং ওয়েটেজ কলাম যোগ করুন।
         টেক সেক্টরের কোম্পানিগুলো ফিল্টার করে দেখান।

টাস্ক ২: একটি CSV ফাইল তৈরি করুন যাতে ৫টি স্টকের ১০ দিনের
         (তারিখ, ওপেন, হাই, লো, ক্লোজ, ভলিউম) ডেটা আছে।
         pd.read_csv() দিয়ে লোড করুন। প্রতিটি স্টকের গড়
         ক্লোজ প্রাইস এবং মোট ভলিউম বের করুন।

টাস্ক ৩: উপরের পোর্টফোলিও DataFrame-এ 'বার্ষিক রিটার্ন (%)'
         নামে একটি কলাম যোগ করুন (যেকোনো মান সেট করতে পারেন)।
         যে কোম্পানির রিটার্ন ১০% এর বেশি, তাদের দেখান।
         কোম্পানির নাম অনুযায়ী DataFrame সর্ট করুন।
""")
